({
    handleClose : function(component, event, helper) {
        helper.handleClose(component);
    },
    
    handleAccept : function(component,event, helper){
        helper.handleRedirect(component);
    } 
})